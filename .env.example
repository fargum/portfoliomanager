# Environment variables for Docker Compose
# SECURITY NOTICE: 
# 1. Copy this file to .env and update with your actual values
# 2. NEVER commit .env file to version control
# 3. The .env file is already added to .gitignore for security

# Database Configuration
DB_HOST=host.docker.internal
DB_PORT=5432
DB_NAME=portfoliomanager
DB_USERNAME=your_actual_username_here
DB_PASSWORD=your_secure_password_here

# EOD Historical Data API Configuration
# Get your API token from https://eodhd.com/
# KEEP THIS SECRET - Never commit actual API tokens!
EOD_API_TOKEN=your_eod_api_token_here

# ASP.NET Core Environment
ASPNETCORE_ENVIRONMENT=Development